---
name: vault-usage
description: Как JARVIS работает с хранилищем файлов и проектов пользователя (~/JARVIS) — поиск по содержимому, чтение PDF/DOCX, работа с кодом проектов.
version: 1.0.0
metadata:
  hermes:
    tags: [jarvis, files, vault, projects]
    category: memory
    requires_toolsets: [jarvis_brain]
---
## Что такое хранилище
`~/JARVIS` — папка пользователя. Он кладёт туда что угодно (документы, PDF, таблицы, заметки) и подключает целые проекты
(`projects/<имя>` — символические ссылки на реальные папки). Индекс содержимого лежит в brain.db и обновляется сам каждые
несколько минут; `vault_manage reindex` — обновить прямо сейчас (например, «я только что положил файл»).

## Алгоритм
1. Вопрос про файлы/документы/проект/код → `vault_search(query)`; для конкретного проекта — `in: "projects/<имя>"`.
   Формулируй запрос ключевыми словами по содержанию («срок договора Acme», «функция логина bcrypt»), не по имени файла.
2. Нашёл → `vault_read(path)` (PDF/DOCX/PPTX/XLSX извлекаются в текст; длинные — по `next_offset`) или штатный `read_file`.
3. Отвечай с указанием источника: «в `inbox/договор Acme.md`, строка 4: …».
4. Изменить файл — `write_file`/`patch` по пути из результата; выполнить/собрать проект — `terminal` внутри `projects/<имя>`
   (это реальная папка пользователя, работай аккуратно: git status до и после, не удаляй без подтверждения).
5. Ничего не нашлось → `vault_manage list` / `tree`, чтобы понять, что вообще есть; если пусто — объясни, как добавить:
   «положите в ~/JARVIS/inbox» или «`jarvis vault add ~/Projects/foo`». Не выдумывай содержимое.

## Запись и порядок (vault_manage)
- `write(path, content, mode)` — создать/изменить текстовый файл **внутри** хранилища или проекта (заметка, конспект, README,
  код). Для существующих файлов проектов предпочитай `patch` — он точечный.
- «Разложи inbox» / «наведи порядок»: `list prefix=inbox` → для каждого файла реши папку по содержимому
  (`inbox/договоры`, `inbox/счета`, `inbox/статьи`, `inbox/личное`…; создавай через `mkdir`, названия — как у пользователя, если
  он уже завёл такие) → `move(path, to)`. Покажи план **до** перемещения, если файлов больше 5; после — краткий отчёт.
- `trash(path)` — только в Корзину и только по явной просьбе. `rm -rf` в хранилище не используй никогда.
- Каждый новый файл получает резюме в базе знаний: `pending` → `vault_read` → `brain_remember(kind='document',
  content='<имя>: <суть>', tags='vault')` → `summarized(file_id, note_id)`. Так «что у меня есть про Acme?» отвечается
  из памяти без повторного чтения, а ссылка на файл — в заметке.
- Подключить стандартные папки: `connect what=icloud|desktop|documents|downloads|notes-obsidian` (Obsidian находится сам по
  конфигу приложения). Предлагай это, когда пользователь ищет файл, которого нет в хранилище («может, он в Документах?
  Подключить ~/Documents?»).

## Границы
- `.env`, ключи, сертификаты, `node_modules`, `.git` и бинарники не индексируются; секреты в тексте скрыты — не пытайся их восстановить.
- Хранилище — источник фактов для ответа, но НЕ база знаний: устойчивые выводы («договор с Acme до конца 2026») при
  необходимости записывай в `brain_remember` со ссылкой на файл.
