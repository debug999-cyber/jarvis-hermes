# ═══════════════════════════════════════════════════════════════════════════
#  J.A.R.V.I.S. — фрагмент конфигурации Hermes Agent
#  Установщик (install.sh) аккуратно ВЛИВАЕТ эти ключи в ~/.hermes/config.yaml,
#  не затирая то, что вы настроили сами (ваши значения имеют приоритет).
#  Полная справка по ключам: https://hermes-agent.nousresearch.com/docs/user-guide/configuration
# ═══════════════════════════════════════════════════════════════════════════

# ── Плагины JARVIS (обязательно, иначе инструменты не загрузятся) ──────────
plugins:
  enabled:
    - jarvis-core
    - jarvis-macos
    - jarvis-brain
  stream_reasoning_deltas: false
  hermes-memory-store:                 # настройки провайдера holographic (файл $HERMES_HOME/memory_store.db)
    auto_extract: "false"              # факты извлекает JARVIS сам (brain_remember + авто-захват «запомни…»), а не regex провайдера
    default_trust: 0.5
  entries:
    jarvis-core:
      settings:
        hud_url: "http://127.0.0.1:8765"
        user_name: "сэр"
        city: "Zürich"
        inject_context: true
        watchdog: true                   # локальные проверки батареи без LLM
        battery_threshold: 20
        watch_calendar: false            # true → напоминать о событиях за 10 минут + справка из базы знаний на HUD
        follow_focus: true               # Focus macOS (Не беспокоить/Работа/Сон) → режим JARVIS
        alerts_webhook: ""               # доставка триггеров в мессенджер через Hermes webhook (см. блок platforms.webhook ниже),
                                         # напр. "http://127.0.0.1:8644/webhooks/jarvis-alerts"
        alerts_secret: ""                # тот же секрет, что у маршрута (или переменная JARVIS_ALERTS_SECRET)
    jarvis-brain:
      settings:
        inject_context: true             # релевантные знания в каждом ходе
        mirror_facts: true               # каждая заметка → факт в памяти Hermes (memory.provider: holographic)
        context_limit: 5
        min_score: 1.2
        log_turns: true                  # журнал ходов для ночного дневника
        auto_capture: true               # «запомни …» сохраняется даже без вызова инструмента
        export_path: ""                  # пусто → $HERMES_HOME/jarvis/BRAIN.md
        backup_mirror: ""                # напр. "~/Library/Mobile Documents/com~apple~CloudDocs/JARVIS" — зеркало бэкапа в iCloud
        log_failures: true               # журнал сбоев инструментов → ночная самодиагностика
        stt_vocabulary: true             # имена из базы → подсказка Whisper
        voice_polish: true               # голосовой ответ без markdown, до 4 предложений
    jarvis-macos:
      settings:
        allow_raw_applescript: false     # включите, если доверяете модели произвольные скрипты
        screenshot_dir: ""               # пусто → $HERMES_HOME/cache/jarvis/screenshots
        default_player: "auto"           # auto | music | spotify

# ── Голос: запись с микрофона в CLI/TUI ────────────────────────────────────
voice:
  record_key: "ctrl+b"
  max_recording_seconds: 120
  auto_tts: true                 # включать озвучку сразу при старте voice mode
  beep_enabled: true
  silence_threshold: 200
  silence_duration: 2.0          # быстрее реагирует, чем дефолтные 3.0
  stop_phrases: ["стоп", "хватит", "спасибо, всё", "stop"]

# ── Распознавание речи (STT) — локально, бесплатно, без ключей ─────────────
stt:
  enabled: true
  provider: "local"              # faster-whisper; альтернативы: groq | openai | mistral | xai
  local:
    model: "small"               # tiny | base | small | medium | large-v3  (small — лучший баланс для русского)
    language: "ru"               # ISO-639-1; пусто = автоопределение

# ── Синтез речи (TTS) — Edge TTS бесплатно; ElevenLabs/OpenAI — по ключу ───
tts:
  provider: "edge"               # edge | elevenlabs | openai | gemini | piper | kittentts | neutts
  speed: 1.05
  edge:
    voice: "ru-RU-DmitryNeural"  # мужской русский; варианты: ru-RU-SvetlanaNeural, en-GB-RyanNeural (классический «Jarvis»)
  elevenlabs:
    voice_id: "pNInz6obpgDQGcFmaJgB"
    model_id: "eleven_multilingual_v2"
  openai:
    model: "gpt-4o-mini-tts"
    voice: "onyx"
  piper:
    voice: "ru_RU-dmitri-medium"

# ── Wake word «Hey Jarvis» — полностью локально (openWakeWord) ─────────────
wake_word:
  enabled: true
  surface: auto                  # auto | cli | tui | gui
  provider: openwakeword         # openwakeword | sherpa (любая фраза) | porcupine
  phrase: "hey jarvis"
  sensitivity: 0.6
  confirmation_frames: 3
  start_new_session: false       # продолжать текущий диалог, а не начинать новый
  openwakeword:
    model: hey_jarvis            # встроенная модель openWakeWord
  porcupine:
    keyword: jarvis

# ── Инструменты: что доступно агенту в CLI (остальные платформы — по своим пресетам)
# Полный список: hermes tools
toolsets:
  hermes-cli:
    - web
    - terminal
    - file
    - browser
    - vision
    - image_gen
    - tts
    - skills
    - memory
    - session_search
    - cronjob
    - todo
    - clarify
    - code_execution
    - delegation
    - computer_use               # Hermes Computer Use (cua-driver): клики/ввод в фоне без кражи курсора — вместо нашего mac_type
    - jarvis_core
    - jarvis_macos
    - jarvis_brain

# ── Computer Use: фоновое управление окнами (Hermes + cua-driver) ──────────
computer_use:
  permission_mode: standard      # standard — обычные подтверждения Hermes; bounded — манифест возможностей

# ── Память и контекст ──────────────────────────────────────────────────────
memory:
  enabled: true
  nudge_interval: 20             # напоминать себе сохранять память каждые N ходов
  provider: holographic          # официальный провайдер Hermes: локальный SQLite FTS5 + trust, инструменты fact_store/fact_feedback.
                                 # Заметки JARVIS (brain_remember) зеркалятся туда; старые переносит `jarvis brain migrate`.

# ── Безопасность: подтверждение опасных команд ────────────────────────────
approvals:
  mode: smart                    # manual | smart | off
  timeout: 120

# ── Терминал ───────────────────────────────────────────────────────────────
terminal:
  backend: local
  cwd: "~"
  timeout: 180

# ── Компрессия контекста (для длинных голосовых сессий) ───────────────────
compression:
  enabled: true
  threshold: 0.75

# ── Cron: модель для фоновых задач (можно дешевле основной) ───────────────
cron:
  preflight: true

# ── Алерты триггеров в мессенджер (диск, питание, входящие файлы, брифинг) ──
# Готовый механизм Hermes: webhook-маршрут с deliver_only доставляет текст без вызова модели (<1 c).
# Включить: в ~/.hermes/.env  WEBHOOK_ENABLED=true  WEBHOOK_PORT=8644  (или `hermes gateway setup`),
# затем в jarvis-core.settings задать alerts_webhook и alerts_secret. Telegram можно заменить на
# discord | slack | signal | whatsapp | matrix | email — любой подключённый в Hermes мессенджер.
# Быстрая альтернатива без правки YAML:
#   hermes webhook subscribe jarvis-alerts --deliver telegram --deliver-only --prompt "{text}" --description "JARVIS alerts"
#   hermes webhook test jarvis-alerts --payload '{"text":"проверка"}'
platforms:
  webhook:
    extra:
      routes:
        jarvis-alerts:
          description: "Уведомления JARVIS (триггеры без LLM)"
          secret: ""                     # пусто → маршрут выключен; совпадает с jarvis-core.settings.alerts_secret
          deliver: telegram
          deliver_only: true
          prompt: "🤖 JARVIS · {event}\n{text}"
