# Использование J.A.R.V.I.S.

## Команда `jarvis`

Первое, что стоит сделать после установки: **`jarvis selftest`** — прогоняет все 29 mac_* инструментов в режиме чтения и
печатает таблицу «✔ работает / ⚠ нет прав (какую панель открыть) / ✖ сломано». `jarvis selftest --fix` открывает панели сам.
Затем **`jarvis brain import`** — JARVIS познакомится с вашими контактами и проектами (с подтверждением).

| Команда | Что делает |
|---|---|
| `jarvis` | голосовой TUI Hermes с профилем JARVIS (wake word + Ctrl+B) |
| `jarvis chat` | классический CLI (`hermes chat`) |
| `jarvis ask "вопрос"` | один вопрос — один ответ, без интерактива (удобно в скриптах/Shortcuts) |
| `jarvis hud` / `hud stop` / `hud restart` / `hud status` / `hud log` | HUD в браузере (если HUD под launchd — команды управляют агентом launchd) |
| `jarvis gateway` / `gateway stop` / `gateway restart` | мессенджеры + OpenAI-совместимый API (`:8642`) |
| `jarvis hush` | немедленно замолчать: `say`, `afplay`, озвучка HUD |
| `jarvis up` | gateway + HUD + TUI одной командой |
| `jarvis status` | состояние компонентов |
| `jarvis brief` | брифинг прямо сейчас |
| `jarvis brain [stats\|review\|export\|diary\|profile\|log\|backup\|restore\|sql\|path]` | база знаний: статистика, ревизия сейчас, выгрузка в Markdown, дневник, журнал изменений, бэкап/откат. `brain sql` открывает базу **только для чтения** (случайный DELETE невозможен); запись — осознанно `brain sql --rw` |
| `jarvis doctor [--fix\|--quick\|--json]` | самодиагностика: Hermes, **модель реально отвечает**, плагины, API-сервер, gateway, HUD, launchd, права macOS, хранилище, версия. `--fix` чинит сам (ключ API, включает плагины, поднимает сервисы, создаёт ~/JARVIS); `--hermes` = сырой `hermes doctor` |
| `jarvis vault [status\|open\|add <папка>\|remove\|list\|tree\|search <слова>\|read <файл>\|reindex]` | **хранилище файлов и проектов** `~/JARVIS` — см. [docs/VAULT.md](VAULT.md) |
| `jarvis shortcuts` | собрать и импортировать Быстрые команды: «Спросить JARVIS» (Siri), «В хранилище JARVIS» (правый клик в Finder), «Замолчать», брифинг, heartbeat |
| `jarvis perms` | открыть панели разрешений macOS |
| `jarvis selftest [--fix]` | проверить все mac_* инструменты на этом Mac (без LLM); `--fix` открывает панели недостающих прав |
| `jarvis brain import` | первичный импорт контактов/проектов в базу знаний |
| `jarvis heartbeat` | одна тихая проверка по `HEARTBEAT.md` (то, что cron делает каждые 45 мин) |
| `jarvis update [--check\|--status\|--rollback\|--force\|--channel\|--auto\|--hermes]` | автообновление JARVIS с GitHub (см. docs/APP.md); `--hermes` — обновить сам Hermes |
| `jarvis app [open\|build\|quit\|status\|debug]` | приложение строки меню JARVIS.app |
| `jarvis version` | версии JARVIS и Hermes |
| `jarvis config` | открыть config.yaml |
| `jarvis logs` | хвост логов |
| `jarvis <любая команда hermes>` | прозрачно передаётся в `hermes` (`jarvis cron list`, `jarvis skills list`…) |

`jarvis hush` — заглушить речь немедленно (то же: «стоп» голосом, Esc в HUD, «Замолчать» в меню ◉).

## Slash-команды в чате

| Команда | Источник | Описание |
|---|---|---|
| `/voice on\|off\|tts\|status` | Hermes | голосовой режим |
| `/wake on\|off\|status` | Hermes | wake word «Hey Jarvis» |
| `/brief` | jarvis-core | утренний/вечерний брифинг |
| `/focus [off\|night\|presentation]` | jarvis-core | режимы |
| `/timer 10 чай` · `/timer 07:30 подъём` · `/timer` | jarvis-core | таймер / будильник / список |
| `/remember <факт> #тег` | jarvis-brain | записать в базу знаний |
| `/recall <тема>` | jarvis-brain | поиск по базе знаний |
| `/brain stats\|entities\|diary\|profile\|history\|failures\|log\|export\|maintain\|review\|restore` | jarvis-brain | состояние/карточки/дневник/профиль/история фактов/сбои/журнал/выгрузка/уборка/ревизия/откат |
| `/jarvis` | skill-bundle | загрузить все навыки JARVIS в сессию |
| `/screen [окно]` | jarvis-macos | скриншот |
| `/vol 30` · `/vol up` · `/vol mute` | jarvis-macos | громкость |
| `/sysinfo [disk\|memory\|…]` | jarvis-macos | сводка о системе |
| `/lock` | jarvis-macos | заблокировать экран |
| `/heartbeat every 15m …` | Hermes | периодическое напоминание в этой же сессии |
| `/cron add "every 2h" "…"` | Hermes | фоновая задача |
| `/personality concise` | Hermes | временный оверлей стиля поверх SOUL.md |
| `/plugins` · `/skills` · `/tools` | Hermes | что загружено |
| `/learn <источник>` | Hermes | сделать навык из документации/репозитория/URL |
| `/new` · `/title …` · `/model` | Hermes | сессии и модель |

## Примеры фраз (что говорить)

### Приложения и окна
- «Открой Safari» · «Закрой Telegram» · «Какие приложения запущены?»
- «Открой youtube.com в Chrome» · «Открой папку Загрузки» · «Найди файл отчёт за квартал»
- «Окно влево» · «Разверни на весь экран» · «Сверни окно»
- «Напечатай: Добрый день, коллеги» · «Нажми cmd+S» · «Скопируй это в буфер»

### Система
- «Громкость тридцать» · «Потише» · «Выключи звук» · «Сделай ярче» · «Включи тёмную тему»
- «Заблокируй экран» · «Усыпи Mac» · «Выключи компьютер» *(переспросит)*
- «Сколько заряда?» · «Сколько места на диске?» · «Что грузит процессор?» · «Какой у меня IP?»
- «Удали этот файл» *(только в Корзину)* · «Переименуй отчёт в final.pdf» · «Перенеси скриншоты в Документы» · «Что в Загрузках?»
- «Выключи Wi-Fi» · «Включи Bluetooth» · «Какие наушники подключены?»

### Экран и камера
- «Что у меня на экране?» → скриншот + vision
- «Прочитай ошибку в терминале и объясни»
- «Посмотри в камеру — я нормально выгляжу для созвона?»

### Музыка
- «Включи музыку» · «Следующий трек» · «Что играет?» · «Включи плейлист Focus» · «Включи Radiohead в Spotify»

### Календарь, напоминания, заметки
- «Что у меня сегодня?» · «Какие планы на завтра?» · «Что 15 сентября?»
- «Поставь встречу с Иваном завтра в 15:00 на час»
- «Напомни завтра в 9 позвонить маме» · «Что в напоминаниях?» · «Отметь “купить молоко” выполненным»
- «Запиши в заметки: идея для проекта — …» · «Найди заметку про отпуск»

### Таймеры и режимы
- «Таймер на 10 минут» · «Разбуди в 7:30» · «Отмени таймер»
- «Режим фокуса» · «Ночной режим» · «Обычный режим» · «У меня презентация»

### HUD

**Озвучка** (клавиша `T`): HUD спрашивает `POST /api/tts` — сервер синтезирует речь тем же голосом, что voice mode Hermes
(edge-tts из config.yaml → `tts.edge.voice`, кэш `$HERMES_HOME/cache/tts/`); если движка нет — озвучит браузер. Esc / ■ / `jarvis hush` обрывают.
- «Покажи на экране видео про то, как работает арк-реактор»
- «Выведи на HUD график продаж: январь 10, февраль 14, март 9»
- «Покажи картинку с последнего скриншота справа» · «Очисти экран»

### Интернет, исследование, код
- «Разберись, что нового в Swift 6, и покажи сводку на экране»
- «Найди три лучших статьи про RAG и сохрани в заметки»
- «Склонируй репозиторий X, поставь зависимости и запусти тесты»
- «Сделай скрипт, который каждый вечер архивирует папку Documents»
- «Открой сайт, залогинься и вытащи таблицу» *(browser toolset)*

### Файлы и проекты (хранилище ~/JARVIS)
- «Что в договоре с Acme про сроки?» · «Найди в моих файлах пароль от роутера… ой, он скрыт — покажи, где лежит» → `vault_search` + `vault_read`
- «Открой проект atlas и объясни, как там сделана авторизация» · «Запусти тесты в проекте atlas» → `projects/atlas` — обычные `read_file`/`terminal`
- «Я положил в inbox презентацию — сделай из неё тезисы» · «Что нового в хранилище?» *(JARVIS сам замечает новые файлы в inbox)*

### Экран
- «Что у меня на экране?» · «Посмотри сюда, что это за ошибка?» · «Переведи текст на экране» → скриншот снимается сам, модель сразу вызывает `vision_analyze`

### Память и база знаний
- «Запомни: мою собаку зовут Рекс» · «Анна теперь тимлид Atlas, пиши ей коротко» → `brain_remember`
- JARVIS подсказал факт из памяти, а вы ответили «это не так» / «уже неактуально» → уверенность заметки падает, JARVIS уточняет и исправляет; «верно» — повышает
- «Что ты знаешь про проект Atlas?» · «Кто такая Анна?» · «Как я обычно деплою?» → `brain_recall` / карточки
- «Это уже не так — Анна ушла из Atlas» · «Забудь про Bun» → `brain_forget`
- «Что было вчера?» → дневник (эпизоды) · «Наведи порядок в памяти» → ревизия сейчас
- «Как я вчера настраивал nginx?» *(session_search по сырым логам)*
- «Сделай из этого навык» · «/learn https://docs.example.com/api»

### Фоновые задачи
- «Каждое утро в восемь присылай брифинг в Telegram»
- «Каждые полчаса проверяй, отвечает ли мой сервер, и пиши только если упал»
- «Через два часа напомни выключить духовку»

## Проактивность без болтовни

| Механизм | Что делает | Стоимость |
|---|---|---|
| **Watchdog** (jarvis-core) | батарея, встречи за 10 минут **со справкой из базы знаний** на HUD, синхронизация с Focus macOS | 0 вызовов LLM |
| **Триггеры** (jarvis-core, каждые 60 с) | реагируют на *события*, а не на таймер: новые файлы в `~/JARVIS/inbox` → JARVIS читает и предлагает, что сделать; **вернулись к Mac** после ≥ 90 мин (первый раз утром → брифинг, иначе — тихая проверка); мало места на диске; выдернули питание при ≤ 30 % | 0 (уведомления) или 1 вызов по конкретному поводу, не чаще раза в 15 мин |
| **Heartbeat** (`jarvis heartbeat`, cron каждые 3 ч — страховка) | читает `~/.hermes/jarvis/HEARTBEAT.md`, проверяет пункты, отвечает `NO_REPLY`, если всё спокойно; помнит, о чём уже предупреждал | 1 короткий вызов |
| **Утренний verify** | 1 сомнительный факт из базы уточняется в конце брифинга | в составе брифинга |

`HEARTBEAT.md` — обычный список; правьте руками или «JARVIS, добавь в heartbeat: напоминать пить воду после 15:00».
Настройки триггеров — `plugins.jarvis-core` в config.yaml: `triggers`, `trigger_llm` (false = только уведомления), `disk_min_gb`, `idle_return_min`. В режимах focus/night срабатывает только срочное.
Отключить heartbeat: `hermes cron remove <id>` или `JARVIS_HEARTBEAT=0 bash ~/.hermes/jarvis/setup_cron.sh` до установки.

### Focus macOS ↔ режим JARVIS
Включили «Не беспокоить»/«Работа» — JARVIS сам переходит в `focus` (только срочное); «Сон» → `night`; выключили → `normal`.
Читается `~/Library/DoNotDisturb/DB` (терминалу может понадобиться «Полный доступ к диску»; если доступа нет — просто ничего
не происходит). Управлять Focus *из* JARVIS: создайте Быструю команду «JARVIS Focus Работа» с действием «Установить фокус» —
`mac_focus set` вызывает её.

## Режимы (`jarvis_mode`)

| Режим | Поведение агента | Побочные эффекты |
|---|---|---|
| `normal` | обычный | — |
| `focus` | минимум слов, никакой инициативы | Shortcut «JARVIS Mode focus» (или «JARVIS Focus On»), если создан — например, включает «Не беспокоить» |
| `night` | тихо, кратко | Shortcut «JARVIS Mode night», если создан |
| `presentation` | не трогает окна/звук/уведомления | Shortcut «JARVIS Mode presentation», если создан |

Режим сохраняется между сессиями и подмешивается в контекст каждого хода.

## HUD

HUD — рабочий стол JARVIS в браузере (`jarvis hud` → http://127.0.0.1:8765). Три колонки на **реальных данных**:

| Виджет | Откуда данные | Что можно делать |
|---|---|---|
| **Сегодня** | Calendar.app (AppleScript, обновляется раз в 2 мин) | «Все события» — спросить JARVIS про сегодня/завтра |
| **Знания** | `brain.db` плагина jarvis-brain | открыть базу знаний, поиск |
| **Система** | `pmset` (батарея), Focus macOS, `config.yaml` (модель), `install.json` (версия) | переключить режим фокуса JARVIS |
| **Таймеры** | общий `state.json` с `jarvis_timer` | добавить/отменить таймер прямо в HUD; срабатывает даже без запущенного агента |

Если Календарь показывает «нет доступа» — Системные настройки → Конфиденциальность и безопасность → Автоматизация → разрешите Terminal (или JARVIS) управлять Calendar.

| Клавиша | Действие |
|---|---|
| **Пробел (удерживать)** / клик по сфере | говорить (Web Speech API браузера; локальный Whisper — в голосовом режиме TUI) |
| **Enter** | отправить текст |
| **K** | база знаний |
| **A** | лента активности агента (tool calls, панели, фон) |
| **T** | озвучивать ответы голосом браузера |
| **H** | спрятать/показать боковые колонки (только сфера и диалог) |
| **?** | подсказка по клавишам |
| **Esc** | замолчать (пока JARVIS говорит/думает) · иначе очистить диалог и закрыть панели |

Deep links: `/#brain`, `/#activity`. Для скриншотов и разработки: `python3 hud/server.py --demo` (виджеты с демо-данными).
- Панели: агент вызывает `jarvis_hud` (kind: text/markdown/image/video/web/chart; position: left/center/right; ttl).
- Лента слева — все tool calls в реальном времени (в т.ч. из Telegram/Discord через gateway-хук).
- Открыть с телефона в той же сети: запустите HUD с `JARVIS_HUD_HOST=0.0.0.0` (только в доверенной сети!).

## Cron-задачи JARVIS (создаются установщиком)

```bash
hermes cron list
```
| Имя | Расписание | Что делает |
|---|---|---|
| JARVIS: утренний брифинг | 08:00 | погода, календарь, напоминания, батарея |
| JARVIS: вечерний итог | 21:00 | что сделано, что перенести, завтра |
| JARVIS: ночная ревизия базы знаний | 03:30 | уборка, дневник дня, слияние дубликатов, таксономия, карточки, отчёт (см. docs/BRAIN.md) |
| JARVIS: обслуживание памяти | вс 20:00 | чистка MEMORY.md / USER.md, перенос устойчивых фактов в базу знаний |
| JARVIS: heartbeat | каждые 45 мин | чек-лист `~/.hermes/jarvis/HEARTBEAT.md`; ответ `NO_REPLY` = ничего не отправляется |

Контроль батареи и напоминания о встречах делает **watchdog** внутри `jarvis-core` — локально, без вызовов LLM
(настройки `battery_threshold`, `watch_calendar`).

Доставка результатов — туда, откуда создана задача (CLI → лог; из Telegram → в Telegram).
Изменить: `hermes cron edit <id> …`, удалить: `hermes cron remove <id>`.

## Личность

`~/.hermes/SOUL.md` — отредактируйте, чтобы поменять характер/обращение/язык.
`plugins.entries.jarvis-core.settings.user_name` — как JARVIS вас называет.
`/personality <preset>` — временный оверлей на сессию.

## Горячие клавиши TUI

| | |
|---|---|
| `Ctrl+B` | начать/остановить запись голоса |
| `Esc` | прервать ответ |
| `Ctrl+C` ×2 | выход |
